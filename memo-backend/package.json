{
  "name": "memo-backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
"scripts": {
  "start": "node index.js",
  "dev": "node --watch index.js",
  "test": "echo \"Error: no test specified\" && exit 1",
  "build:ui": "if exist build rmdir /s /q build && cd ../muistiinpanot && npm run build && xcopy /E /I /Y build ../memo-backend/build",
  "deploy:full": "npm run build:ui && git add . && git commit -m \"uibuild\" && git push"
},
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.12.0"
  }
}
